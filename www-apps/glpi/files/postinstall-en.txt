0. Create a new MySQL database:
mysqladmin create geeklog

1. Login on
http://${VHOST_HOSTNAME}/${VHOST_APPDIR}/
and follow the directions.

2. Don't forget to delete the install directory when you're done!

3. For security reason, please set permission on  config/config_db.php
	to 400 ( is owner you web serwer user)
